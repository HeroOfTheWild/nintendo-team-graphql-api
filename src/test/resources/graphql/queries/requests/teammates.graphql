query {
   teammates(teamId: "nintendo01") {
       nintendoId
       teamId
       teamInfo {
           managerId
           teamName
       }
       name {
           firstName
           middleName
           lastName
       }
   }
}